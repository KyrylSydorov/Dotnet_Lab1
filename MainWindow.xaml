<Window x:Class="Sydorov_Lab1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Розрахунок віку" Height="350" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Дата народження:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker SelectedDate="{Binding BirthDate, Mode=TwoWay}" />
        </StackPanel>
        <Button Grid.Row="1" Content="Розрахувати" Command="{Binding CalculateCommand}" Margin="0,0,0,10" Width="120" />
        <TextBlock Grid.Row="2" Text="{Binding AgeDisplay}" Margin="0,0,0,10" FontSize="16"/>
        <TextBlock Grid.Row="3" Text="{Binding BirthdayMessage}" Margin="0,0,0,10" FontSize="16" Foreground="Green"/>
        <StackPanel Grid.Row="4" Orientation="Vertical">
            <TextBlock Text="{Binding WesternZodiacDisplay}" FontSize="14"/>
            <TextBlock Text="{Binding ChineseZodiacDisplay}" FontSize="14"/>
        </StackPanel>
    </Grid>
</Window>
